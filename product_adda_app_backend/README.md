# Run Only This Test from PowerShell

Open your PowerShell terminal at your project root (`$home\vs-code\product_adda\product_adda_app_backend`) and run this command:

```powershell
./mvnw test -Dtest=HashUtilTest

```
